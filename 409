class Solution(object):
    def longestPalindrome(self, s):
        seen = set()
        length = 0

        for ch in s:
            if ch in seen:
                seen.remove(ch)
                length += 2
            else:
                seen.add(ch)

        return length + (1 if seen else 0)
